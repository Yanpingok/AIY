name: Trigger pre-build images for Aiy commits

on:
  workflow_dispatch:
  push:
    branches: 
      - 'main'
      - 'devnet'
      - 'testnet'
      - 'mainnet'
      - 'releases/aiy-*-release'

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  prebuild-aiy-images:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch Pre-build Images in MystenLabs/aiy-operations
        uses: peter-evans/repository-dispatch@ff45666b9427631e3450c54a1bcbee4d9ff4d7c0 # pin@v3.0.0
        with: 
          repository: MystenLabs/aiy-operations
          token: ${{ secrets.DOCKER_BINARY_BUILDS_DISPATCH }}
          event-type: prebuild-aiy-images
          client-payload: >
            {
              "aiy_commit": "${{ github.sha }}",
              "caller_name": "aiy_repo_${{ github.ref_name }}",
              "push_to_dockerhub": true,
              "build_arm64": ${{ github.ref_name != 'main' && (github.ref_name == 'devnet' || github.ref_name == 'testnet' || github.ref_name == 'mainnet' || (startsWith(github.ref_name, 'releases/aiy-') && endsWith(github.ref_name, '-release'))) }},
              "graphql_rpc": true,
              "indexer_alt": true,
              "stress": true
            }
