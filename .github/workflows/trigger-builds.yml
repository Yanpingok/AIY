name: Trigger Release binaries builds
permissions:
  contents: read

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
      - 'devnet'
      - 'testnet'
      - 'mainnet'
      - 'releases/aiy-*-release'

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release-binaries:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch Release binaries builds in MystenLabs/aiy-operations
        uses: peter-evans/repository-dispatch@ff45666b9427631e3450c54a1bcbee4d9ff4d7c0 # pin@v3.0.0
        with:
          repository: MystenLabs/aiy-operations
          token: ${{ secrets.DOCKER_BINARY_BUILDS_DISPATCH }}
          event-type: build-release-binaries
          client-payload: '{"aiy_commit": "${{ github.sha }}"}'

  # Tag all aiy images with branch names, so that they can be easily found
  tag-docker-hub-images:
    name: Tag aiy docker images in DockerHub
    if: github.event_name == 'push' && !startsWith(github.ref_name, 'releases') && github.ref_name != 'main'
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch Tagging of images in DockerHub, in MystenLabs/aiy-operations
        uses: peter-evans/repository-dispatch@ff45666b9427631e3450c54a1bcbee4d9ff4d7c0 # pin@v3.0.0
        with:
          repository: MystenLabs/aiy-operations
          token: ${{ secrets.DOCKER_BINARY_BUILDS_DISPATCH }}
          event-type: tag-docker-images
          client-payload: '{"aiy_commit": "${{ github.sha }}", "tag": "${{ github.ref_name }}"}'
