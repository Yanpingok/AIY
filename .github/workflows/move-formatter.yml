# Checks formatting in all examples and system packages (TBD for Aiy System and Bridge)
# Uses a fixed NPM version to separate prettier-move release changes from system package
# changes.
#
# With new releases of prettier-move, update the `VERSION` in the env.
name: Move Formatter

on:
  push:
    branches: main
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

env:
  VERSION: 0.3.0

jobs:
  prettier-move:
    name: Check Move formatting
    runs-on: ubuntu-latest
    strategy:
      matrix:
        working-directory:
          - examples # run on all examples
          - crates/aiy-framework/packages/aiy-framework # aiy framework
          - crates/aiy-framework/packages/move-stdlib # move stdlib
          - crates/aiy-framework/packages/aiy-system # aiy system
          - crates/aiy-framework/packages/bridge # bridge
    defaults:
      run:
        working-directory: ${{ matrix.working-directory }}
    steps:
      - uses: actions/checkout@v5
      - name: Use Node.js
        uses: actions/setup-node@v4
      - run: npm install --no-warnings @mysten/prettier-plugin-move@${{ env.VERSION }}
      - run: npx prettier-move -c **/*.move
